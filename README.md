# Vehicle Rental Management System

A simple vehicle rental management application built using **Java**.  
The user interface is implemented with **JavaFX**, and data persistence is handled via the **GSON** library for JSON serialization.

Developed in **IntelliJ IDEA**.
